# TIL

#나의 오답
print(list(numbers(4,10)))

numbers = range(1, 10)
# numbers는 range 객체
numbers(...) → # ❌ 함수처럼 호출함
##👉 range 객체는 호출 불가

#정답
print(list(numbers[3:11]))

## 4는 인덱스 3
## 10은 포함 안 되니까 11 → 인덱스 10
